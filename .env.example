# Local development should use a local Postgres database.
# Vercel production should use a separate Neon Postgres database.
DATABASE_URL="postgresql://localhost:5432/debt_crusher_local"

# Required site-wide gate for both local and Vercel.
BASIC_AUTH_USERNAME="change-me"
BASIC_AUTH_PASSWORD="change-me-too"
