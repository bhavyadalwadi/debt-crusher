import { clerkMiddleware } from "@clerk/nextjs/server";

// Clerk authenticates requests here. Authorization stays next to each protected
// page or API resource so a route cannot accidentally inherit the wrong policy.
export default clerkMiddleware();

export const config = {
  matcher: [
    "/((?!_next|[^?]*\\.(?:html?|css|js(?!on)|jpe?g|webp|png|gif|svg|ttf|woff2?|ico|csv|docx?|xlsx?|zip|webmanifest)).*)",
    "/(api|trpc)(.*)",
    "/__clerk/:path*",
  ],
};
